import { StyleSheet } from "react-native";
import theme from "../theme";

export default StyleSheet.create({
  container: {
    backgroundColor: "#EF6D4E",
    fontSize: 12,
    tintColor: theme.colors.white
  },
  image: {
    tintColor: theme.colors.white
  }
});
